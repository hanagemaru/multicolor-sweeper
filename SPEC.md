# Multicolor Sweeper 製品仕様

## ゲーム構成

- 盤面は9×9固定
- Time Attackのみ
- 難易度はEASY 15爆弾 / NORMAL 20爆弾 / HARD 25爆弾
- START前に3色または4色を選ぶ
- 同じ候補Seed・初手・attemptでは、3色/4色の爆弾位置を共通にする
- Daily Challengeは現時点では対象外

## 盤面生成

- START後、盤面内の好きなセルを初手として選べる
- 初手セルと周囲8セルは必ず安全
- 初手タップ後、module Web Worker内で生成・Solver判定を行う
- 製品版の採用フィルタは条件C
  - 3色・4色ともNo-Guess
  - color-essential（多色では解け、同じ配置を単色化すると論理で詰まる）
  - 4色のreasoning round数が3色以下
- 生成開始から120ms経過した場合だけ短い生成中表示を出す
- タイマーは採用盤面の生成と初手開封が完了した後に開始する

## 標準Solver

各未開封セルは `safe / red / blue / green / yellow` のdomainを持つ。3色ではyellowを
含めない。プレイヤーに見えているClueと、それまでの論理的な確定結果だけから、次の制約を
収束まで反復する。

1. 各Clueの色別爆弾数
2. 各Clueの全色合計爆弾数
3. 盤面全体の総爆弾数
4. 隣接Clue間のsubset/difference
5. 確定状態を反映した各制約の残数

確率、ランダム手、隠れた正解を推論規則に使わない。隠れた盤面はsoundness assertionと、
確定safeを開くシミュレーションにだけ使う。

## 操作

- タップ：未開封セルを開く
- 開封済みClueをタップ：条件を満たす場合にChord
- スワイプ方向は、開始地点から20pxに初めて到達した時点で固定
- 3色モード
  - ↖ 赤旗
  - ↗ 青旗
  - ↙ 緑旗
  - ↑ 無色旗
  - ↘ は未使用（旗を立てず、タップ開封にも戻さない）
- 4色モード
  - ↖ 赤旗
  - ↗ 青旗
  - ↙ 緑旗
  - ↘ 黄旗
  - ↑ 無色旗
- 同じ旗を同じセルへ再度付けると解除する
- 別の旗を同じセルへ付けると色を変更し、旗の本数自体は変わらない
- 旗の設置本数に上限は設けず、爆弾総数を超えても新しい旗を置ける
- 盤面の色数に存在しない色旗はゲーム内部でも受け付けない

### Chordの混合判定

隣接する旗の総数がClueの合計と一致し、かつ各色旗の数がその色のClueを超えていない場合、
無色旗を未確定の残り色として扱い、旗のない隣接セルを一括開封する。誤った位置へ旗を置いて
いた場合は通常のマインスイーパーと同様に爆発し得る。

## Seed再現性

- 候補Seedは `${baseSeed}|attempt:${attempt}`
- 不採用時はattemptを1ずつ増やす
- 同じbaseSeed、初手、爆弾数、フィルタ、最大試行回数から、同じ候補列と採用盤面を生成する
- PRNG、位置決定、色袋の順序はLab版と同じ意味を維持する
- Seed / attempt / 生成時間は内部再現性・検証用の情報であり、通常のプレイ画面には表示しない

## 表示・フォント

- 日本語：MaruMonica
- 英語、タイマー、爆弾数、マス内数字：VT323
- フォントの出典とライセンス表記は `public/fonts/` に保存する
- 爆弾を表す英語は `BOMBS`、残り旗数は `FLAGS` に統一し、日本語では「爆弾」「旗」と表記する
- アクセシビリティ上の一般名称としての「マインスイーパー盤面」は変更しない
- 難易度選択は `15 BOMBS` / `20 BOMBS` / `25 BOMBS` と表示する
- プレイ中HUDは `TIME` と `FLAGS` の2項目とし、爆弾総数の項目は追加しない。ユーザー向けの `MINES` 表示は使用しない
- `FLAGS` は「爆弾総数 - 現在置かれている旗の総数」。爆弾総数を超えて旗を置いた場合は負数になり、警告色で表示する
- 現在の画面は機能確認用の最小UI。Gradient Sweeperの背景・グラディエント・完成デザインは踏襲しない
- 初手待ち・盤面生成中・プレイ中は `100dvh` 内に収め、盤面は利用可能な幅と高さの短辺に合わせる
- 初手待ち・盤面生成中・プレイ中は操作ガイド領域を含む同じレイアウト高さを維持し、盤面生成の前後で盤面サイズを変えない
- 120ms経過後の生成中表示は盤面上にオーバーレイし、周囲のレイアウト高さを変えない
- 設定画面と結果画面は縦スクロール可能なままにする

## ランキング（後続）

- 3難易度×3色/4色の6部門
- バックエンド、認証、不正対策は後続セッションで決める

## 技術構成

- React / Vite / TypeScript
- PWA（生成Service Worker、manifest、maskable含むPNGアイコン一式）
- 盤面生成・Solverはmodule Web Worker

## 配信

- 配信先は Cloudflare Workers（Static Assets）。静的成果物は `dist/` に出力し、`wrangler.jsonc` がそのまま配る
- 公開URLは `https://mcsweeper.hanage.app/`（カスタムドメイン割り当て前は `*.workers.dev` の既定URLで配信）
- hanage-hub の方針どおり製品ごとに独立したサブドメインを持ち、ハブ側には紹介ページのみを置いて別タブで開く
- 専用ドメイン前提のため Vite の `base` と manifest の `start_url` / `scope` はいずれも `/`。サブパス配信にするとService Workerのscopeとアセットの絶対パスが崩れる
- ヘッダーは `public/_headers` で指定（manifestのContent-Type、`X-Frame-Options` 等）

## 今回の対象外

- ランキング、認証、広告
- Daily Challenge
- 完成デザイン、演出、サウンド
- 実機固有の最終QA
