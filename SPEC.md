# Multicolor Sweeper 製品仕様

最終更新: 2026-09-04

## ゲーム構成

- 盤面は9×9固定
- Time Attackのみ
- 難易度はEASY 15爆弾 / NORMAL 20爆弾 / HARD 25爆弾
- START前に3色または4色を選ぶ
- 同じ候補Seed・初手・attemptでは、3色/4色の爆弾位置を共通にする
- Daily Challengeは現時点では対象外

## 盤面生成

- START後、盤面内の好きなセルを初手として選べる
- 初手セルと周囲8セルは必ず安全
- 初手タップ後、module Web Worker内で生成・Solver判定を行う
- 製品版の採用フィルタは条件C
  - 3色・4色ともNo-Guess
  - color-essential（多色では解け、同じ配置を単色化すると論理で詰まる）
  - 4色のreasoning round数が3色以下
- 生成開始から120ms経過した場合だけ短い生成中表示を出す
- タイマーは採用盤面の生成と初手開封が完了した後に開始する
- 通常のセル開封とChordではタイマーを継続し、CLEARまたはGAME OVERが確定した時だけ停止する

## 標準Solver

各未開封セルは `safe / red / blue / green / yellow` のdomainを持つ。3色ではyellowを含めない。プレイヤーに見えているClueと、それまでの論理的な確定結果だけから、次の制約を収束まで反復する。

1. 各Clueの色別爆弾数
2. 各Clueの全色合計爆弾数
3. 盤面全体の総爆弾数
4. 隣接Clue間のsubset/difference
5. 確定状態を反映した各制約の残数

確率、ランダム手、隠れた正解を推論規則に使わない。隠れた盤面はsoundness assertionと、確定safeを開くシミュレーションにだけ使う。

## 操作

- タップ：未開封セルを開く
- 開封済みClueをタップ：条件を満たす場合にChord
- スワイプ方向は、開始地点から20pxに初めて到達した時点で固定
- 3色モード
  - ↖ 赤旗
  - ↗ 青旗
  - ↙ 緑旗
  - ↑ 無色旗
  - ↘ は未使用（旗を立てず、タップ開封にも戻さない）
- 4色モード
  - ↖ 赤旗
  - ↗ 青旗
  - ↙ 緑旗
  - ↘ 黄旗
  - ↑ 無色旗
- 同じ旗を同じセルへ再度付けると解除する
- 別の旗を同じセルへ付けると色を変更し、旗の本数自体は変わらない
- 旗の設置本数に上限は設けず、爆弾総数を超えても新しい旗を置ける
- 盤面の色数に存在しない色旗はゲーム内部でも受け付けない
- 盤面上のタッチ/スワイプはブラウザの履歴戻る等のネイティブナビゲーションよりゲーム操作を優先し、左端セルの旗スワイプでページ遷移させない

### Chord判定

隣接する旗の総数がClueの合計と一致する場合、旗の色やClueの色別内訳にかかわらず、旗のない隣接セルを一括開封する。誤った位置へ旗を置いていた場合は通常のマインスイーパーと同様に爆発し得る。

## ポーズ

- タイムアタックのプレイ中HUDには `PAUSE` を表示する
- PAUSE中はタイマーを停止し、RESUME後は停止時間を加算しない
- PAUSE中は実際の盤面状態を保持するが、画面上では全81セルを未開封セルの見た目に置き換える
- PAUSE中はClue、旗、爆弾、開封位置を見せず、盤面操作も受け付けない
- PAUSE画面は冗長な説明文を置かず、`PAUSE` と主要操作 `RESUME` / `再開`、補助操作 `MENU` だけを表示する
- `MENU` は記録を送信せず開始画面へ戻る
- ポーズ開始・解除で盤面の位置・サイズを変えない

## Seed再現性

- 候補Seedは `${baseSeed}|attempt:${attempt}`
- 不採用時はattemptを1ずつ増やす
- 同じbaseSeed、初手、爆弾数、フィルタ、最大試行回数から、同じ候補列と採用盤面を生成する
- PRNG、位置決定、色袋の順序はLab版と同じ意味を維持する
- Seed / attempt / 生成時間は内部再現性・検証用の情報であり、通常のプレイ画面には表示しない

## UIデザイン（Board First）

UIレイアウト・文字・配色の品質改善では、完成案C「Board First」を採用する。

- 盤面を主役とし、装飾より可読性・タップ性・小画面適応を優先する
- `MULTICOLOR SWEEPER` と `TIME ATTACK` は設定画面だけに表示する
- プレイ中は `TIME` / `FLAGS` / `PAUSE` を横一列のHUDとして配置し、左右の偏りをなくす
- 難易度と色数は盤面直上の補助情報として簡潔に表示する
- 縦長画面でもHUDと盤面の間に大きな空白を作らず、余剰高さは原則として盤面より下側へ逃がす
- 日本語グリフ：MaruMonica
- 英字・数字、タイマー、爆弾数、マス内数字：言語モードにかかわらずVT323
- 日本語モードは `VT323 → MaruMonica` のフォントスタックとし、英数字と日本語が同じ文言内に混在しても文字種ごとに一貫したフォントを使う
- 混在する短いラベルは共通の基準サイズと行高で揃え、フォント切替による不自然な大小差や縦ずれを避ける
- 設定画面とランキング画面に `日本語 | EN` の言語切替を横一列で表示し、選択を端末に保存する
- 日本語モードでもゲーム界隈で一般的な短い英語は残し、説明・状態・ランキング固有語を日本語化する。詳細は `UI_TERMINOLOGY.md` を正とする
- 操作案内・説明・エラーは選択中の言語だけを表示し、英日併記はしない
- 操作ボタンは可能な限り約44px以上のタップ領域を確保する
- 低い画面でも主要ラベルを極端に縮小せず、320×480を最低基準として扱う
- 最小文字は日本語・補足文12px、VT323の英字・数字14pxを目安とし、高さ不足は文字ではなく余白・間隔を優先して調整する
- 背景、パネル、罫線、文字、アクセントは共通の配色体系で管理する
- ゲーム内の赤・青・緑・黄の意味・色定義は `rules.ts` の `COLORS` を唯一の出処として維持する
- 既存の爆弾・旗・矢印・Clueのピクセルアートは維持する
- 色だけに依存せず、文字、形、枠、位置でも状態を識別できるようにする
- フォント読み込み前後でも盤面・主要操作の寸法をフォントだけに依存させない

## viewport / 盤面レイアウト

- 設定、初手待ち、生成中、プレイ中、結果、ポーズを `100dvh` 内に収め、ページ全体の縦スクロールを発生させない
- 盤面は常に9×9の正方形
- 初手待ち・生成中・プレイ中・ポーズで盤面の位置とサイズを変えない
- 盤面は利用可能な幅と高さの短辺に合わせる
- 120ms経過後の生成中表示は盤面上にオーバーレイし、周囲のレイアウト高さを変えない
- 320×480 / 320×568 / 375×667 / 390×844程度を主要検証サイズとする

## 結果表示

- CLEAR / GAME OVER / 生成エラーはゲーム画面全体のモーダル層として表示し、盤面位置を変えない
- CLEARではクリアタイムを最も強く表示する
- CLEAR時の主要操作は `RETRY`
- 記録未送信時の第二操作は英語 `SUBMIT TIME` / 日本語 `タイムを登録`
- 送信成功後の第二操作はランキング閲覧
- `VIEW BOARD` と `MENU` は補助操作として一段弱く表示する
- GAME OVERでは `RETRY` を主要操作とし、`VIEW BOARD` / `MENU` を補助操作にする。敗北時タイムは表示しない
- 生成エラーではエラー内容、`RETRY`、`MENU`を表示する
- `VIEW BOARD` で結果を閉じると決着後の答え合わせを確認できる
- 閉じた後の `RESULT` はHUD右端の操作枠へ表示し、盤面の位置・サイズを変えない
- 盤面確認中の状態文言 `CLEAR!` / `GAME OVER` は盤面下の既存ステータス領域へ表示する

## 演出・効果音

- 通常開封は約140msの軽い縮小→ポップと約275msの黄色い斜めスキャンで表示し、演出中も次の盤面入力を受け付ける
- 連鎖開封はゲーム状態を即時確定したうえで、起点からの距離ごとに21msずつ、開封ポップを含む全体が約300ms以内になる見た目だけの遅延を付ける。13マス以上では開封完了付近に盤面全体の小さな追いポップを加える
- 開封音は1〜4マスまで開いたマス数と同じ回数を鳴らす。5マス以上は最大6回の短い上昇連打と2〜3音の終止和音へ圧縮し、約250ms以内で開封規模を表現する
- 爆発は約220msの高い予兆音と一瞬の静けさから、強いフラッシュ・28粒子・低い衝撃音・全セルが外側へ吹き飛ぶCINEMATIC BLASTへつなぐ
- 通常のCLEARは完成盤面を見せてから左上→右下へ波状にポップする現行演出を使う。自己ベスト更新時は中心から全セルを強くポップし、光条・16スパーク・大きな上昇和音を重ねるSUPER CLEARを使う
- 旗の設置・色変更には短く硬い音、同じ旗を外す操作には少し高く軽い音を鳴らす。旗の正誤によって音を変えない
- 効果音は外部アセットを使わずWeb Audio APIで生成し、開封・連鎖・旗・爆発・CLEARを同じ現代的なピクセルゲーム調へ揃える
- Web Audioは最初の盤面操作で有効化し、音の再生や演出待ちでゲームロジックと入力をブロックしない
- `prefers-reduced-motion: reduce` ではセル・盤面・粒子のアニメーションを停止し、結果表示までの待ちも短縮する

## オンラインランキング

ランキングはTime Attackの主要機能として、Cloudflare Workers + D1でオンライン化する。

### 部門

- ランキング部門は爆弾数ごとの3部門
  - 15 BOMBS
  - 20 BOMBS
  - 25 BOMBS
- 3色と4色は同じ部門へ混在させる
- プレイヤーは3色/4色の得意な方でより良いタイムを狙える
- ランキング表には `COLORS` 列を設け、各記録が3色か4色か明示する
- 自己ベストも同じ爆弾数なら3色/4色をまたいで比較する
- 同一player・同一部門では最速のverified recordだけをランキング対象とする
- ランキング取得は上限件数を設け、V1は最大50件。UIは上位10件を基本表示する

### プレイヤー識別・名前

- ゲーム開始前の名前登録は必須にしない
- 名前未登録でも通常プレイ・再挑戦できる
- 匿名player ID + ランダムcredentialを端末に保持し、ログインを要求しない
- credential本体をDBへ保存せず、サーバーはSHA-256ハッシュで照合する
- 初めて `SUBMIT TIME` / `タイムを登録` を選んだ時だけ名前登録を求める
- 一度登録した表示名は端末内にも保持する
- 設定画面とランキング画面から後で名前を登録・変更できる
- 保存済みの旧表示名は引き継いでよい
- メールアドレスやSNSアカウントはV1では収集しない
- 将来の任意アカウントへ匿名playerの記録を引き継げる構造を維持する

### 記録送信

- CLEAR時にローカル自己ベスト更新なら `NEW BEST!` を表示する
- 自己ベスト未更新時は、今回の `CLEAR TIME` の下に従来の自己ベストを `BEST TIME` として小さく表示する
- 自己ベスト更新時だけ記録を送信できる。未更新時はランキング閲覧のみ
- 登録操作には `SUBMITTING...` / 成功 / 失敗の状態を持つ
- 成功時は今回の順位を表示する
- 自己ベスト更新時は登録成功後に結果を約1秒表示してからランキングへ自動遷移する
- 登録失敗時は結果に留まり、再試行できる
- 失敗時も結果、RETRY、MENU、VIEW BOARDを失わない
- 二重送信はsubmission IDで冪等化する
- 旧localStorageのモックランキング/テスト記録はオンラインへ自動送信しない

### 保存データ

最低限、次を保存する。

- 匿名player ID
- 表示名
- 爆弾数
- 色数
- クリアタイム
- base Seed
- 初手位置
- 採用attempt
- rule version
- app version
- open / flag操作履歴とゲーム内経過時間
- 登録/更新日時
- verification status

### V1サーバー検証

- API入力の型・値域をすべてサーバー側で検証する
- 爆弾数は15/20/25、色数は3/4のみ受け付ける
- 表示名はNFC正規化、最大16文字、制御文字と `<` / `>` を拒否する
- `baseSeed + attempt + mineCount + colorCount + firstClick` から盤面を決定論的に再生成する
- 初手開封とopen/flag操作をproduction game coreでreplayし、Chordを含めCLEAR成立を確認する
- 爆弾ヒット、未CLEAR、CLEAR後の追加操作、不正な時系列、異なるrule/app versionを拒否する
- 異常に短いタイムは通常ランキングへ載せず隔離する
- 記録送信、名前更新、削除へplayer/IP系シグナルのレート制限を設ける
- IPアドレスそのものはDBへ保存せず、レート制限用にハッシュ化したシグナルだけを使う
- 管理者権限、D1の秘密情報、Cloudflare API tokenをブラウザへ渡さない
- Workers FreeのCPU制限を優先し、V1のscore submitでは標準Solverを再実行して条件Cまで証明しない。条件Cは製品クライアントの盤面生成時に適用し、サーバー側は盤面再現とCLEAR成立を検証する

### 将来の不正対策強化

- サーバー発行のone-time run ticket
- サーバー側開始時刻と期限
- signed board metadataまたはサーバー側のより強い盤面適格性検証
- 必要に応じCloudflare Rate Limiting binding等へ強化

### ランキング画面

- Board First方針ではランキングは独立画面とする
- 設定画面とCLEAR結果からランキングへ入れる
- 15/20/25 BOMBSを画面内で切り替えられる
- 上位記録と `YOUR RANK` を表示する
- 表の主要列は英語時 `RANK / NAME / COLORS / TIME`。日本語時は順位・名前・色数を日本語化し、`TIME` は英語のまま維持する
- 設定画面から開いた場合は `BACK` / `戻る` だけを表示する
- CLEAR結果から開いた場合は `RESULT` と `MENU` を表示する
- 通信失敗時はランキング取得失敗を表示するが、ゲーム本体へ影響させない

### 削除・プライバシー

- `DELETE /api/player` で認証済み匿名playerとオンライン記録を削除できる構造にする
- 利用者向けプライバシーポリシーはハブの `https://hanage.app/privacy/` を正とする
- ハブのポリシーには、匿名識別子の目的、プレイ/検証データ、ハッシュ化ネットワークシグナル、Cloudflare利用、保存/削除を記載する
- 設定画面からプライバシーポリシーを開ける導線を、広告導入前に追加する
- アプリ内のオンラインデータ削除UIが完成するまでは、ハブのお問い合わせページから削除依頼を受け付ける

## 広告

- 全作品共通の方針は、hanage-hub の `docs/ADVERTISING_POLICY.md` を正とする
- 現在は広告を配信せず、今回の実装対象にも含めない
- 導入する場合も、盤面の操作中・タイム計測中には表示しない
- 結果画面など自然な区切りのうち、具体的な表示タイミング、頻度、二重表示防止を実装前にこの仕様書へ追記する
- 広告取得の失敗、同意拒否、広告ブロックによってゲーム開始・結果確認・RETRY・MENUを妨げない

## 多言語化

- 設定画面とランキング画面に `日本語 | EN` の言語切替を表示する
- 選択言語はlocalStorageへ保存する。未保存時はブラウザ言語を参照する
- 日本語モードは全訳せず、日本のゲームUIで一般的な短い英語はそのまま残す
  - 例: `TIME ATTACK` / `START` / `RANKING` / `TIME` / `FLAGS` / `PAUSE` / `MENU` / `CLEAR!` / `CLEAR TIME` / `RETRY` / `NEW BEST!` / `RESULT`
- 意味を考えやすい説明、状態、ランキング固有語は日本語化する
  - 例: `SUBMIT TIME` → `タイムを登録`、`YOUR RANK` → `あなたの順位`、`COLORS` → `色数`
- 操作案内・説明・エラーは選択中の言語だけを表示する
- 日本語はMaruMonica、英語・数字・共通ゲーム英語はVT323を基本とする
- 表記の正は `UI_TERMINOLOGY.md` とする
- ゲームタイトル `MULTICOLOR SWEEPER` は固有名詞として両言語で維持する
- 実行中HTML `lang` は選択言語へ追従する

## 技術構成

- React / Vite / TypeScript
- PWA（生成Service Worker、manifest、maskable含むPNGアイコン一式）
- 盤面生成・Solverはmodule Web Worker
- Cloudflare WorkerがStatic Assetsと `/api/*` を同居させる
- ランキングDBはCloudflare D1
- production D1とPR preview D1を分離する

## 配信

- 配信先は Cloudflare Workers（Static Assets + ranking API）
- 静的成果物は `dist/` に出力し、`wrangler.jsonc` のAssetsから配る
- `/api/*` はWorkerコードを先に実行する
- 公開URLは `https://mcsweeper.hanage.app/` を予定（カスタムドメイン割り当て前は `*.workers.dev`）
- 専用ドメイン前提のため Vite の `base` と manifest の `start_url` / `scope` はいずれも `/`
- ヘッダーは `public/_headers` で指定する
- D1 migrationは `migrations/` で管理する
- PR Previewは本番トラフィックを変更せずpreview D1へ接続する

## 今回の対象外

- メール/SNSアカウント認証
- サーバー発行run ticketを使うV2不正対策
- score submit時の標準Solver/条件C完全再検証
- 振動
- ゲームルール・難易度・旗操作の変更
- 広告
- エンドレスモード
- No-Guess生成ロジック変更
- 爆弾・旗アートの作り直し
