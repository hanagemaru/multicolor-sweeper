# Multicolor Sweeper 製品仕様

最終更新: 2026-08-30

## ゲーム構成

- 盤面は9×9固定
- Time Attackのみ
- 難易度はEASY 15爆弾 / NORMAL 20爆弾 / HARD 25爆弾
- START前に3色または4色を選ぶ
- 同じ候補Seed・初手・attemptでは、3色/4色の爆弾位置を共通にする
- Daily Challengeは現時点では対象外

## 盤面生成

- START後、盤面内の好きなセルを初手として選べる
- 初手セルと周囲8セルは必ず安全
- 初手タップ後、module Web Worker内で生成・Solver判定を行う
- 製品版の採用フィルタは条件C
  - 3色・4色ともNo-Guess
  - color-essential（多色では解け、同じ配置を単色化すると論理で詰まる）
  - 4色のreasoning round数が3色以下
- 生成開始から120ms経過した場合だけ短い生成中表示を出す
- タイマーは採用盤面の生成と初手開封が完了した後に開始する

## 標準Solver

各未開封セルは `safe / red / blue / green / yellow` のdomainを持つ。3色ではyellowを含めない。プレイヤーに見えているClueと、それまでの論理的な確定結果だけから、次の制約を収束まで反復する。

1. 各Clueの色別爆弾数
2. 各Clueの全色合計爆弾数
3. 盤面全体の総爆弾数
4. 隣接Clue間のsubset/difference
5. 確定状態を反映した各制約の残数

確率、ランダム手、隠れた正解を推論規則に使わない。隠れた盤面はsoundness assertionと、確定safeを開くシミュレーションにだけ使う。

## 操作

- タップ：未開封セルを開く
- 開封済みClueをタップ：条件を満たす場合にChord
- スワイプ方向は、開始地点から20pxに初めて到達した時点で固定
- 3色モード
  - ↖ 赤旗
  - ↗ 青旗
  - ↙ 緑旗
  - ↑ 無色旗
  - ↘ は未使用（旗を立てず、タップ開封にも戻さない）
- 4色モード
  - ↖ 赤旗
  - ↗ 青旗
  - ↙ 緑旗
  - ↘ 黄旗
  - ↑ 無色旗
- 同じ旗を同じセルへ再度付けると解除する
- 別の旗を同じセルへ付けると色を変更し、旗の本数自体は変わらない
- 旗の設置本数に上限は設けず、爆弾総数を超えても新しい旗を置ける
- 盤面の色数に存在しない色旗はゲーム内部でも受け付けない

### Chord判定

隣接する旗の総数がClueの合計と一致する場合、旗の色やClueの色別内訳にかかわらず、旗のない隣接セルを一括開封する。誤った位置へ旗を置いていた場合は通常のマインスイーパーと同様に爆発し得る。

## ポーズ

- タイムアタックのプレイ中HUDには `PAUSE` を表示する
- PAUSE中はタイマーを停止し、RESUME後は停止時間を加算しない
- PAUSE中は実際の盤面状態を保持するが、画面上では全81セルを未開封セルの見た目に置き換える
- PAUSE中はClue、旗、爆弾、開封位置を見せず、盤面操作も受け付けない
- PAUSE画面の主要操作は `RESUME`、補助操作は `MENU`
- `MENU` は記録を送信せず開始画面へ戻る
- ポーズ開始・解除で盤面の位置・サイズを変えない

## Seed再現性

- 候補Seedは `${baseSeed}|attempt:${attempt}`
- 不採用時はattemptを1ずつ増やす
- 同じbaseSeed、初手、爆弾数、フィルタ、最大試行回数から、同じ候補列と採用盤面を生成する
- PRNG、位置決定、色袋の順序はLab版と同じ意味を維持する
- Seed / attempt / 生成時間は内部再現性・検証用の情報であり、通常のプレイ画面には表示しない

## UIデザイン（Board First）

UIレイアウト・文字・配色の品質改善では、完成案C「Board First」を採用する。

- 盤面を主役とし、装飾より可読性・タップ性・小画面適応を優先する
- `MULTICOLOR SWEEPER` と `TIME ATTACK` は設定画面だけに表示する
- プレイ中は `TIME` / `FLAGS` / `PAUSE` を横一列のHUDとして配置し、左右の偏りをなくす
- 難易度と色数は盤面直上の補助情報として簡潔に表示する
- 日本語：MaruMonica
- 英語、タイマー、爆弾数、マス内数字：VT323
- 通常UIは英語を基本とし、初手待ちとプレイ中の操作案内だけ英日併記する
- 言語切替UIは表示しない。翻訳基盤は保持する
- 操作ボタンは可能な限り約44px以上のタップ領域を確保する
- 低い画面でも主要ラベルを極端に縮小せず、320×480を最低基準として扱う
- 背景、パネル、罫線、文字、アクセントは共通の配色体系で管理する
- ゲーム内の赤・青・緑・黄の意味・色定義は `rules.ts` の `COLORS` を唯一の出処として維持する
- 既存の爆弾・旗・矢印・Clueのピクセルアートは維持する
- 色だけに依存せず、文字、形、枠、位置でも状態を識別できるようにする
- フォント読み込み前後でも盤面・主要操作の寸法をフォントだけに依存させない

## viewport / 盤面レイアウト

- 設定、初手待ち、生成中、プレイ中、結果、ポーズを `100dvh` 内に収め、ページ全体の縦スクロールを発生させない
- 盤面は常に9×9の正方形
- 初手待ち・生成中・プレイ中・ポーズで盤面の位置とサイズを変えない
- 盤面は利用可能な幅と高さの短辺に合わせる
- 120ms経過後の生成中表示は盤面上にオーバーレイし、周囲のレイアウト高さを変えない
- 320×480 / 320×568 / 375×667 / 390×844程度を主要検証サイズとする

## 結果表示

- CLEAR / GAME OVER / 生成エラーはゲーム画面全体のモーダル層として表示し、盤面位置を変えない
- CLEARではクリアタイムを最も強く表示する
- CLEAR時の主要操作は `RETRY`
- 記録未送信時の第二操作は `SAVE SCORE`
- 送信成功後の第二操作は `VIEW RANKING`
- `VIEW BOARD` と `MENU` は補助操作として一段弱く表示する
- GAME OVERでは `RETRY` を主要操作とし、`VIEW BOARD` / `MENU` を補助操作にする。敗北時タイムは表示しない
- 生成エラーではエラー内容、`RETRY`、`MENU`を表示する
- `VIEW BOARD` で結果を閉じると決着後の答え合わせを確認できる
- 閉じた後の `RESULT` は盤面のセルへ重ねず、盤面外の専用領域に表示する

## ランキングUI

ランキングはTime Attackの主要機能としてUI構造を先に実装する。ただし今回の段階では通信・認証・DB・不正対策を実装しない。

### 部門

- ランキング部門は爆弾数ごとの3部門
  - 15 BOMBS
  - 20 BOMBS
  - 25 BOMBS
- 3色と4色は同じ部門へ混在させる
- プレイヤーは3色/4色の得意な方でより良いタイムを狙える
- ランキング表には `COLOR` 列を設け、各記録が3色か4色か明示する
- 自己ベストも同じ爆弾数なら3色/4色をまたいで比較する

### 名前登録

- ゲーム開始前の名前登録は必須にしない
- 名前未登録でも通常プレイ・再挑戦できる
- 初めて `SAVE SCORE` を選んだ時だけ名前登録を求める
- 一度登録した名前は端末内に保持できる
- 設定画面とランキング画面から後で名前を登録・変更できる
- 将来バックエンド接続時も、ゲーム開始自体を名前登録でブロックしない

### 記録送信UI

- CLEAR時にローカル自己ベスト更新なら `NEW BEST!` を表示する
- 保存操作には `SENDING...` / 成功 / 失敗の状態を持つ
- 成功時は今回の順位を表示する
- 失敗時も結果、RETRY、MENU、VIEW BOARDを失わない
- 現段階ではモックランキングとlocalStorageを使い、将来の通信層へ置換可能な構造にする

### ランキング画面

- Board First方針ではランキングは独立画面とする
- 設定画面とCLEAR結果からランキングへ入れる
- 15/20/25 BOMBSを画面内で切り替えられる
- 上位記録と `YOUR RANK` を表示する
- 表の主要列は `RANK / NAME / COLOR / TIME`
- ランキング画面から同じ爆弾数でゲーム開始できる
- 元の設定画面または結果表示へ戻る導線を持つ

## 多言語化

- 現段階の通常UIは英語を基本とし、ユーザー向けの言語切替UIは表示しない
- 初手待ちは `Tap any cell to start` / 「好きなマスをタップしてください」を併記する
- プレイ中は `Tap to open · Swipe to flag` / 「タップで開く・スワイプで旗を立てる」を併記する
- その他の短いラベルは `DIFFICULTY`、`TIME`、`FLAGS`、`PAUSE`、`MENU` など英語表記を維持する
- 日本語 / 英語の翻訳定義、言語検出、保存処理、テストは削除しない
- ゲームタイトル `MULTICOLOR SWEEPER` は固有名詞として維持する
- PWA manifestと初期HTMLの静的metadata、および現在の実行中HTML `lang` は英語を基準にする

## 技術構成

- React / Vite / TypeScript
- PWA（生成Service Worker、manifest、maskable含むPNGアイコン一式）
- 盤面生成・Solverはmodule Web Worker

## 配信

- 配信先は Cloudflare Workers（Static Assets）。静的成果物は `dist/` に出力し、`wrangler.jsonc` がそのまま配る
- 公開URLは `https://mcsweeper.hanage.app/` を予定（カスタムドメイン割り当て前は `*.workers.dev`）
- 専用ドメイン前提のため Vite の `base` と manifest の `start_url` / `scope` はいずれも `/`
- ヘッダーは `public/_headers` で指定する

## 今回の対象外

- 実ランキング通信、ランキングDB
- 認証、不正対策
- 開封・連鎖・爆発・クリアアニメーション
- 効果音、振動
- ゲームルール・難易度・旗操作の変更
- 広告
- エンドレスモード
- No-Guess生成ロジック変更
- 爆弾・旗アートの作り直し
- 言語切替UIの復活
