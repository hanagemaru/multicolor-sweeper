# Project Status

最終更新: 2026-08-30

## 現在地

- React / Vite / TypeScript / PWAの製品版基礎構成を作成済み
- Labの決定論的ゲームコア、標準Solver、color-essential、条件C生成器をTypeScriptへ移植済み
- 生成処理をmodule Web Worker境界へ分離済み
- EASY 15 / NORMAL 20 / HARD 25爆弾、3色/4色選択、自由初手、初手周囲安全、生成後タイマー開始を実装済み
- タップ開封、Chord、3色/4色の色旗・無色旗、勝敗、決着後答え合わせを実装済み
- Cloudflare Workers（Static Assets）へデプロイ済み

## mainの確認状態

2026-08-30時点の最新mainは `cc857836a3cfa122229b97dcda6e68e4151c14bc`。

- PR #16: main反映済み
- PR #17: main反映済み
- PR #18: main反映済み。最新mainはPR #18のマージコミット
- PR #16〜#18反映後の基準テスト数は **44 tests**
- 最新mainのGitHub Actionsで `npm run typecheck` / `npm test` / `npm run build` 成功を確認済み

旧記述の43 testsおよび「#16〜#18作業中」相当の記述はこの更新で廃止する。

## UI品質改善 — 現在作業中

選択済み完成案: **C / Board First**

作業ブランチ: `ui/board-first-ranking-pause`

目的:

- 盤面を主役にしつつ、文字サイズ、フォント、余白、HUD、ボタン階層、配色を一つのデザイン体系へ整理する
- 320×480を含む低い画面でも主要文字・MENU/PAUSE・結果操作を読める/押せる状態にする
- RESULTをセルから外す
- 将来ランキングを接続してもゲーム画面を大幅に作り直さなくてよい画面構造にする

### 実装済み（作業ブランチ、未マージ）

- 英語UIの基本フォントをVT323へ整理し、日本語併記だけMaruMonicaを使用
- プレイ画面を1行HUDへ整理
  - `TIME`
  - `FLAGS`
  - `PAUSE`
- 難易度・色数は盤面直上の補助情報へ移動
- ボタン・パネル・罫線・背景・アクセントを共通の配色トークンへ整理
- 小画面でMENU等を10px、日本語案内を7pxまで縮小していた旧ルールを廃止
- 操作ボタンは原則約44pxのタップ領域を確保する設計へ変更
- `VIEW BOARD` 後の `RESULT` を盤面外の専用領域へ移し、セルとの重なりを解消
- 結果操作の優先順位を整理
  - 第一: `RETRY`
  - 第二: `SAVE SCORE` または送信後 `VIEW RANKING`
  - 補助: `VIEW BOARD` / `MENU`

### PAUSE（作業ブランチ、未マージ）

Gradient SweeperのTime Attackで採用している「タイマー停止＋盤面マスク」の考え方を参照し、多色版のBoard Firstレイアウトへ再設計した。

- プレイ中に `PAUSE` を表示
- PAUSE中はタイマーを停止
- 実際の盤面状態は保持するが、81セルすべてを未開封セルの見た目として描画
- Clue、旗、爆弾、開封位置を見せない
- PAUSE中は盤面操作を無効化
- `RESUME` を主要操作、`MENU` を補助操作にする
- RESUME後は停止時間をタイムへ含めない
- ポーズ前後で盤面サイズ・位置を変えない

### ランキングUI（作業ブランチ、未マージ）

今回実装するのはUI状態とモックデータまで。実通信、DB、認証、不正対策は未実装。

- 設定画面に `RANKING` 導線を追加
- ゲーム開始前の名前登録は必須にしない
- 初回 `SAVE SCORE` 時、未登録なら名前登録ダイアログを出す
- 設定画面とランキング画面から名前を登録/変更可能
- CLEAR時にクリアタイムとローカル自己ベスト更新 `NEW BEST!` を表示
- 記録保存UIに `SENDING...` / 成功 / 失敗状態を用意
- 成功時に今回順位を表示
- ランキングは独立画面
- 部門は `15 / 20 / 25 BOMBS` の3部門
- 3色と4色を同一部門へ混在
- 表に `COLOR` 列を追加し3色/4色を識別
- 表の主要列は `RANK / NAME / COLOR / TIME`
- 自分の順位を表示
- ランキング画面から選択中の爆弾数で再プレイ可能
- モックランキングとlocalStorageのUI境界を、将来APIへ置換できる形で分離

## 自動テスト

main基準: **44 tests**。

作業ブランチではランキングの純粋ロジック用に4 testsを追加済み。

- 3色/4色が同一部門へ混在すること
- タイム昇順で順位付けすること
- 自分の記録を挿入して順位を取得できること
- 名前未登録ではランキング記録を作らないこと

PR作成後のGitHub Actionsで typecheck / 全テスト / production build を最終確認する。成功後に実数を本節へ反映する。

## 既存の確定事項

- 製品版の採用フィルタは条件C
- 条件C / No-Guess / Seed / attempt / generation ms は内部仕様であり通常UIには表示しない
- タイマーはWorker生成完了・初手開封後に開始
- Chordは旗総数判定
- 旗の設置本数は爆弾総数で制限せず、超過時は `FLAGS` を負数の警告表示にする
- 3色は ↖赤 / ↗青 / ↙緑 / ↑無色、↘未使用
- 4色は ↖赤 / ↗青 / ↙緑 / ↘黄 / ↑無色
- 色の定義は `rules.ts` の `COLORS` が唯一の出処
- 通常UIは英語を基本とし、初手待ちとプレイ中の操作案内だけ英日併記
- 言語切替UIは表示しないが翻訳基盤は保持
- `MULTICOLOR SWEEPER` / `TIME ATTACK` は設定画面だけに表示
- ページ全体を100dvhに固定し、盤面は9×9正方形を維持
- 爆弾・旗・矢印はSVG/ピクセルアートで保持

## 検証対象viewport

実装PRでは最低限以下を確認する。

- 320×480
- 320×568
- 375×667
- 390×844

状態:

- 設定
- 初手待ち
- 生成中
- プレイ中
- PAUSE
- CLEAR
- GAME OVER
- VIEW BOARD後のRESULT再表示導線
- ランキング
- 初回名前登録
- 記録送信中 / 成功 / 失敗

## 残課題

- 作業ブランチのCI、画面サイズ別最終QA、PRレビュー
- 実ランキング通信、DB、認証、不正対策
- 開封・連鎖・爆発・クリア演出
- 効果音
- iPhone / Android実機とアクセシビリティ最終QA
- D型色覚での赤-緑の追加確認
- `hanage.app` のCloudflare移管と `mcsweeper.hanage.app` カスタムドメイン割り当て
- hanage-hub側の紹介ページ追加（別Repo）

## 次の順序

1. UIレイアウト・文字・配色の品質改善（現在作業中。ランキングUIシェル・PAUSEを含む）
2. 開封・連鎖・爆発・クリアの演出＋効果音
3. 端末・アクセシビリティ最終QA

実ランキングバックエンド接続はUIシェルとは別タスクとし、演出作業との優先順位は実装時に調整する。
