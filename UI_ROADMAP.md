# UI Roadmap

最終更新: 2026-08-30

このファイルは、ChatGPT Work / Codex / Claude Codeなど複数の実装担当へUI作業を引き継ぐためのタスク一覧です。

## 読み方と情報の優先順位

1. `SPEC.md`: 確定した製品仕様
2. `PROJECT_STATUS.md`: mainに反映済みの実装状況と検証結果
3. `UI_ROADMAP.md`: UI作業の順序、依存関係、現在の担当範囲

矛盾がある場合は推測で進めず、最新のmain、関連PR、上記ファイルを確認すること。

## エージェント向け開始手順

1. 最新のmainを取得する
2. `SPEC.md`、`PROJECT_STATUS.md`、このファイルを読む
3. 対象タスクの前提PRがmainへマージ済みか確認する
4. 1タスクにつき原則1ブランチ・1PRで進める
5. ゲーム仕様やUXを大きく変える判断は、実装前に選択肢と推奨案を提示する
6. 完了時は関連ドキュメントと、このファイルの状態・PR番号を更新する

優先度名だけで仕様を推測しないこと。プロンプトではタスクID、名称、対象範囲を併記する。

## タスク状態

- **DONE**: mainへ反映済み
- **IN PROGRESS**: PR作成済み、未マージ
- **NEXT**: 前提タスクの完了後に着手
- **LATER**: 後続候補。詳細仕様は未確定

## UIタスク

### P0 — 操作整合性とプレイ画面viewport固定

**状態: DONE**

関連PR: #7〜#10

完了内容:

- 3色モードは ↖赤 / ↗青 / ↙緑 / ↑無色。↘は未使用
- 盤面の色数に存在しない旗を内部でも拒否
- 初手待ち・生成中・プレイ中を100dvh内へ固定
- 低い画面で盤面・HUD・操作ガイドが切れないよう調整
- Seed / attempt / generation msを通常UIから削除
- 生成中も操作ガイド領域を維持し、盤面サイズ変化を防止

詳細は `SPEC.md`、`PROJECT_STATUS.md`、PR #7〜#10を参照。

### P1-A — 用語統一・HUD整理

**状態: DONE**

関連PR: #11、#13

確定内容:

- 爆弾の英語表記は `BOMBS`
- 残り旗数は `FLAGS`
- HUDは `TIME` / `FLAGS`
- 旗数超過を許可し、`FLAGS`を負数の警告表示にする
- 難易度表示は `15/20/25 BOMBS`
- 内部のmine系命名は大規模変更しない
- `CONDITION C` / `NO-GUESS` など内部用語を通常UIから外す
- プレイ中に開始画面へ戻る導線は `MENU`

### P1-B — トップ画面固定・結果オーバーレイ

**状態: IN PROGRESS**

前提: P1-A（PR #11）のmain反映

作業ブランチ: `feat/p1b-result-overlay`

目的:

- トップ・設定画面を主要な縦画面の100dvh内に収める
- CLEAR / GAME OVER / 生成エラーを盤面下ではなくオーバーレイ表示する
- 結果表示による縦スクロールと盤面の位置変化を防ぐ
- 既存の決着後答え合わせを残す

予定仕様:

- CLEAR: クリアタイム、`RETRY`、`MENU`、「盤面を見る」
- GAME OVER: `RETRY`、`MENU`、「盤面を見る」。敗北時タイムは表示しない
- 生成エラー: エラー内容、`RETRY`、`MENU`
- 「盤面を見る」で結果オーバーレイを閉じ、答え合わせを表示
- 閉じた後はレイアウトを変えない小さな `RESULT` ボタンから再表示
- 320×480 / 320×568 / 375×667 / 最近のiPhone相当で確認

このタスクには爆発・クリアアニメーション、効果音、ランキングを含めない。

### P1-C — 旗操作・爆発・クリアの視覚フィードバック

**状態: LATER**

候補範囲:

- 旗方向が20pxで固定された瞬間の色プレビュー
- 爆発の短い決着シーケンス
- クリアの短い決着シーケンス
- `prefers-reduced-motion`への配慮

長時間操作を止める演出は避ける。詳細はP1-B完了後に決定する。

### P1-D — 効果音とミュート

**状態: LATER**

候補範囲:

- 開封
- 旗
- 爆発
- クリア
- 常時アクセス可能なミュート設定

音源、音量、保存方法は未確定。

### P1-E — 端末・アクセシビリティ最終QA

**状態: LATER**

候補範囲:

- iPhone実機Safari
- Android Chrome
- 主要viewport
- タッチ操作とブラウザジェスチャーの干渉
- フォーカス、読み上げ、キーボード操作
- D型色覚での赤・緑の追加確認

完成UI・演出・音の実装後に実施する。

## UIロードマップ外の主要課題

以下は別タスクとして扱う。

- 6部門ランキング
- バックエンド、認証、不正対策
- 広告
- カスタムドメイン
- hanage-hubの紹介ページ
- Daily Challenge

## 更新ルール

タスク開始時:

- 状態を `NEXT` から `IN PROGRESS` へ変更
- ブランチ名またはPR番号を追記

タスク完了時:

- mainへのマージ後に `DONE` へ変更
- 実装内容、検証結果、関連PR番号を `PROJECT_STATUS.md` に反映
- 確定した製品仕様を `SPEC.md` に反映
